spring:
  application:
    name: testeurekaorigininvoker
  profiles:
    active:
      default
  cloud:
    config:
      allow-override: true
eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8089/eureka/
server:
  port: 8091